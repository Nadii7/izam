import 'package:flutter/cupertino.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

const primary = Color(0xFF1877F2);
const secondary = Color(0xFF8AC7FF);
const alternate = Color(0xB3FFFFFF);

const primaryText = Color(0xFF12151C);
const secondaryText = Color(0xFF5A5C60);

const primaryBackground = Color(0xFFF4F4F6);
const secondaryBackground = Color(0xFFFFFFFF);

const error = Color(0xFFE65454);
const success = Color(0xFF02CA79);
const warning = Color(0xFFC96F46);

var defaultPadding = EdgeInsets.symmetric(
  vertical: 10.h,
  horizontal: 10.w,
);

var defaultPadding2 = EdgeInsets.symmetric(
  vertical: 5.h,
  horizontal: 5.w,
);

double defaultHeight = kMinInteractiveDimensionCupertino;
